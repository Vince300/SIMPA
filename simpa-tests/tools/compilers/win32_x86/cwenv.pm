#!/usr/bin/env perl

## set any environment, e.g. PATH, default includes, cygwin prefix here
## so we can just call the tools like GCC without explicitily setting them
## link is the exception, so we use linkruntime as a warper to provide runtime,
## startup files, and linker command files
sub setenv {
}

return 1;
